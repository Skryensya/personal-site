---
import siteConfig, { type Link } from '@/data/site-config';
import WaveDivider from './WaveDivider.astro';
import { Heart } from 'lucide-react';
import { getTranslations } from '@/i18n/utils';
import { defaultLang, type Language } from '@/i18n/ui';
import CircularTextAstro from '@/components/CircularTextAstro.astro';
import FooterNavigationList from '@/components/FooterNavigationList.astro';
import ThemeControl from '@/components/ThemeControl.astro';
import ModeControl from '@/components/ModeControl.astro';
import LanguageControl from '@/components/LanguageControl.astro';

interface Props {
  lang?: Language;
}

const { lang = defaultLang } = Astro.props;
const t = getTranslations(lang);

const contactLocalPartByLang: Record<Language, string> = {
  es: 'contacto',
  en: 'contact',
  no: 'kontakt',
  ja: 'renraku',
};
const localizedContactEmail = `${contactLocalPartByLang[lang]}@allison.sh`;

const connectLinks = (siteConfig.socialLinks || []).map((link: Link) => {
  let text = link.text;
  let href = link.href;

  if (link.href.startsWith('mailto:')) {
    text = t('social.email');
    href = `mailto:${localizedContactEmail}`;
  } else if (link.href.includes('github.com')) text = t('social.github');
  else if (link.href.includes('linkedin.com')) text = t('social.linkedin');
  else if (link.href.includes('lichess.org')) text = t('social.lichess');

  return {
    href,
    text,
    isExternal: true,
  };
});

const signatureName = 'Allison Peña';
const byAllisonText = t('footer.byAllison');
const hasSignatureName = byAllisonText.includes(signatureName);
const byAllisonPrefix = hasSignatureName ? byAllisonText.replace(signatureName, '').trim() : '';
const byAllisonName = hasSignatureName ? signatureName : byAllisonText;
const modeLabel = lang === 'es' ? 'Modo' : lang === 'no' ? 'Modus' : lang === 'ja' ? 'モード' : 'Mode';
const themeLabel = lang === 'es' ? 'Tema' : lang === 'no' ? 'Tema' : lang === 'ja' ? 'テーマ' : 'Theme';
const languageLabel = lang === 'es' ? 'Idioma' : lang === 'no' ? 'Språk' : lang === 'ja' ? '言語' : 'Language';
---

<footer id="site-footer" class="bg-main border-t-double border-t-2 border-secondary selection-opposite">
  <WaveDivider inverted />
  <div class="mx-auto max-w-[var(--max-w-size)] px-4 pb-4">
    <div class="flex flex-col md:flex-row gap-4 mb-8">
      <div class="flex flex-col md:flex-row gap-4 md:gap-6 lg:gap-14 w-full lg:flex-1">
        <div class="md:w-[33.33%] w-full">
          <h3 class="font-mono uppercase tracking-wider text-sm font-bold !mb-2 !mt-1 text-secondary scramble-text" data-text={t('footer.navigation')}>
            {t('footer.navigation')}
          </h3>
          <FooterNavigationList lang={lang} />
        </div>

        <div class="md:w-[33.33%] w-full">
          <h3 class="font-mono uppercase tracking-wider text-sm font-bold !mb-2 !mt-1 text-secondary scramble-text" data-text={t('footer.connect')}>
            {t('footer.connect')}
          </h3>
          <FooterNavigationList lang={lang} links={connectLinks} />
        </div>

        <div class="md:w-[33.33%] w-full">
          <div
            data-footer-controls-host
            class="relative min-h-[9.5rem] md:min-h-[10rem]"
            aria-live="polite"
          >
            <div data-footer-controls-placeholder class="space-y-2 pt-1" aria-hidden="true">
              <div class="h-3.5 w-16 border border-secondary/25 bg-secondary/15"></div>
              <div class="h-8 w-full max-w-[220px] border border-secondary/25 bg-secondary/10"></div>
              <div class="h-3.5 w-16 border border-secondary/25 bg-secondary/15"></div>
              <div class="h-8 w-full max-w-[220px] border border-secondary/25 bg-secondary/10"></div>
              <div class="h-3.5 w-20 border border-secondary/25 bg-secondary/15"></div>
              <div class="h-8 w-full max-w-[220px] border border-secondary/25 bg-secondary/10"></div>
            </div>
          </div>

          <template data-footer-controls-template>
            <nav class="space-y-1">
              <div class="block font-mono text-sm pt-0 pb-0">
                <div class="relative text-secondary font-mono text-sm pr-20 md:pr-0">
                  <div class="md:hidden absolute right-0 top-[18px] scale-[1.1] origin-top-right" style="view-transition-name: none;">
                    <CircularTextAstro text="ALLISON PEÑA * SKRYENSYA * " onHover="speedUp" spinDuration={20} />
                  </div>

                  <h3 class="font-mono uppercase tracking-wider text-sm font-bold !mb-1 !mt-1 text-secondary scramble-text" data-text={modeLabel}>
                    {modeLabel}
                  </h3>

                  <div class="mt-1 text-main space-y-2.5">
                    <div class="pb-1">
                      <ModeControl id="footer-mode-control" />
                    </div>

                    <div class="pb-1">
                      <h3 class="font-mono uppercase tracking-wider text-sm font-bold !mb-0.5 !mt-0 text-secondary scramble-text" data-text={themeLabel}>
                        {themeLabel}
                      </h3>
                      <ThemeControl id="footer-theme-control" />
                    </div>

                    <div class="pb-1">
                      <h3 class="font-mono uppercase tracking-wider text-sm font-bold !mb-0.5 !mt-0 text-secondary scramble-text" data-text={languageLabel}>
                        {languageLabel}
                      </h3>
                      <LanguageControl id="footer-language-control" currentPath={Astro.url.pathname} initialLocale={lang} />
                    </div>

                  </div>
                </div>
              </div>
            </nav>
          </template>
        </div>
      </div>

      <div class="hidden lg:flex lg:w-36 justify-center items-start overflow-visible" style="view-transition-name: none;">
        <CircularTextAstro text="ALLISON PEÑA * SKRYENSYA * " onHover="speedUp" spinDuration={20} />
      </div>
    </div>
  </div>

  <div class="mx-auto w-full max-w-[var(--max-w-size)] px-4 pt-2 pb-8">
    <div class="w-full border-t border-secondary/50 pt-2 flex justify-center">
      <p class="font-mono text-sm text-secondary scramble-text flex items-center justify-center gap-2 text-center -translate-y-[5px] pt-1" data-text={`${t('footer.builtWith')} ${t('footer.byAllison')}`} data-has-heart="true">
        <span class="scramble-part">{t('footer.builtWith')}</span>
        <Heart size={14} className="text-secondary" fill="currentColor" />
        {byAllisonPrefix && <span class="scramble-part">{byAllisonPrefix}</span>}
        <span class="scramble-part">{byAllisonName}</span>
      </p>
    </div>
  </div>
</footer>

<script>
  const debugLogger = (window as any).debugLogger ?? {
    log: () => {},
    warn: () => {},
    group: () => {},
    groupEnd: () => {}
  };

  let footerCleanup: (() => void) | null = null;

  function initFooter() {
    if (footerCleanup) {
      footerCleanup();
      footerCleanup = null;
    }

    debugLogger.log('🦶 Footer script initialized');

    let footerAnimated = false;
    const cleanupFns: Array<() => void> = [];
    const scrambleTimers = new WeakMap<HTMLElement, number>();

    function scrambleText(element: HTMLElement, originalText: string, duration: number = 450): void {
      const chars = 'abcdefghijkstuvwxyzABCDEFGHWXYZ01234!@#$%&*:.?';
      let i = 0;
      const stepMs = 50;
      const maxSteps = Math.ceil(duration / stepMs);

      const existing = scrambleTimers.get(element);
      if (existing) {
        clearInterval(existing);
        scrambleTimers.delete(element);
      }

      const timer = window.setInterval(() => {
        const progress = Math.min(i / maxSteps, 1);
        const reveal = Math.floor(progress * originalText.length);

        element.textContent = originalText
          .split('')
          .map((_ch: string, idx: number) => (idx < reveal ? originalText[idx] : chars[Math.floor(Math.random() * chars.length)]))
          .join('');

        i++;
        if (progress >= 1) {
          clearInterval(timer);
          scrambleTimers.delete(element);
          element.textContent = originalText;
        }
      }, stepMs);

      scrambleTimers.set(element, timer);
    }

    function scrambleContainer(container: HTMLElement): void {
      const parts = container.querySelectorAll('.scramble-part');
      if (parts.length) {
        parts.forEach((part: Element, idx: number) => {
          const el = part as HTMLElement;

          if (!el.dataset.originalText) {
            el.dataset.originalText = (el.textContent || '').trim();
          }

          const txt = el.dataset.originalText || '';
          const delay = idx * 120;
          if (!txt.trim()) return;
          setTimeout(() => scrambleText(el, txt), delay);
        });
      } else {
        if (!container.dataset.originalText) {
          container.dataset.originalText = (container.dataset.text || container.textContent || '').trim();
        }

        const txt = container.dataset.originalText || '';
        if (!txt.trim()) return;
        if (container.children.length === 0) {
          scrambleText(container, txt);
        }
      }
    }

    const mountFooterControls = () => {
      const host = document.querySelector('[data-footer-controls-host]') as HTMLElement | null;
      const template = document.querySelector('[data-footer-controls-template]') as HTMLTemplateElement | null;

      if (!host || !template || host.dataset.mounted === 'true') return;

      host.innerHTML = '';
      host.appendChild(template.content.cloneNode(true));
      host.dataset.mounted = 'true';

      requestAnimationFrame(() => {
        const controlWindow = window as Window & {
          __initializeDropdowns?: () => void;
          __enhanceDropdownItems?: () => void;
          modeControl?: { refresh?: () => void };
          languageControl?: { refresh?: () => void };
          themeControl?: { refresh?: () => void };
        };

        controlWindow.modeControl?.refresh?.();
        controlWindow.languageControl?.refresh?.();
        controlWindow.themeControl?.refresh?.();
        controlWindow.__enhanceDropdownItems?.();
        controlWindow.__initializeDropdowns?.();
      });
    };

    const runFooterScramble = (footerEl: Element) => {
      if (footerAnimated) return;
      footerAnimated = true;

      window.dispatchEvent(new CustomEvent('footer-scramble-start'));

      const elements = footerEl.querySelectorAll('.scramble-text');
      elements.forEach((el: Element, idx: number) => {
        setTimeout(() => scrambleContainer(el as HTMLElement), idx * 28);
      });
    };

    const scrambleObserver = new IntersectionObserver((entries: IntersectionObserverEntry[]) => {
      for (const entry of entries) {
        if (entry.isIntersecting) {
          setTimeout(() => runFooterScramble(entry.target), 80);
          scrambleObserver.disconnect();
          break;
        }
      }
    }, { threshold: 0.05, rootMargin: '100px 0px' });

    const footer = document.getElementById('site-footer');
    if (footer) {
      scrambleObserver.observe(footer);
    }

    const controlsHost = document.querySelector('[data-footer-controls-host]') as HTMLElement | null;
    if (controlsHost) {
      const controlsObserver = new IntersectionObserver((entries: IntersectionObserverEntry[]) => {
        for (const entry of entries) {
          if (entry.isIntersecting) {
            mountFooterControls();
            controlsObserver.disconnect();
            break;
          }
        }
      }, { threshold: 0, rootMargin: '240px 0px' });

      const eagerMount = () => mountFooterControls();

      controlsObserver.observe(controlsHost);
      controlsHost.addEventListener('pointerenter', eagerMount, { once: true });
      controlsHost.addEventListener('focusin', eagerMount, { once: true });

      cleanupFns.push(() => {
        controlsObserver.disconnect();
        controlsHost.removeEventListener('pointerenter', eagerMount);
        controlsHost.removeEventListener('focusin', eagerMount);
      });
    }

    cleanupFns.push(() => {
      scrambleObserver.disconnect();
    });

    footerCleanup = () => {
      cleanupFns.forEach((fn) => fn());
    };
  }

  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', initFooter, { once: true });
  } else {
    initFooter();
  }

  const footerWindow = window as Window & { __footerPageLoadBound?: boolean };
  if (!footerWindow.__footerPageLoadBound) {
    document.addEventListener('astro:page-load', initFooter);
    footerWindow.__footerPageLoadBound = true;
  }
</script>

<style>
  .text-body-mono {
    font-family: var(--font-mono);
    font-size: var(--fs-body);
    line-height: var(--lh-normal);
  }
</style>
