---
import type { CollectionEntry } from 'astro:content';
import { getTranslations } from '@/i18n/utils';
import { defaultLang, type Language } from '@/i18n/ui';
import { getLocalizedUrl } from '@/utils/localized-routes';
import { getSlugFromId } from '@/utils/content-utils';
import SevenSegmentDisplay from '@/components/ui/seven-segment/SevenSegmentDisplay.astro';

interface Props {
    projects: CollectionEntry<'projects'>[];
    startIndex?: number;
    lang?: Language;
}

const { projects, startIndex = 0, lang = defaultLang } = Astro.props;
const t = getTranslations(lang);
---

{projects.length > 0 && (
  <div class="w-full">
    <h2 class="h2 mb-8"> 
      {t('home.allProjects')}
    </h2>

    <!-- Header - Desktop only -->
    <div class="hidden md:flex gap-4 border-b border-main/30  py-2 px-6 bg-main items-center selection-opposite">
      <div class="flex-[0_0_30%] flex gap-6 items-center">
        <div class="font-mono text-sm font-bold uppercase tracking-wider text-secondary w-[50px]">#</div>
        <div class="font-mono text-sm font-bold uppercase tracking-wider text-secondary flex-1">{t('projects.table.name')}</div>
      </div>
      <div class="flex-1">
        <div class="font-mono text-sm font-bold uppercase tracking-wider text-secondary">{t('projects.table.description')}</div>
      </div>
      <div class="flex-[0_0_48px]">
        <div class="font-mono text-sm font-bold uppercase tracking-wider text-secondary sr-only">Link</div>
      </div>
    </div>

    <!-- Rows -->
    {projects.map((project: CollectionEntry<'projects'>, index: number) => (
      <a
        href={getLocalizedUrl(lang, 'projects', getSlugFromId(project.id))}
        class="project-row relative overflow-hidden flex flex-col md:flex-row gap-3 md:gap-4 border-b-[1px] border-main/30 border-l-main bg-main/[3%] hover:bg-main/[8%] py-5 md:py-3 px-4 md:px-6 cursor-pointer group font-mono hover:no-underline transition-[background-color,transform,border-color,box-shadow] duration-200 ease-out focus-visible:outline-none focus-visible:bg-main/[10%]"
        style={`--row-index: ${index};`}
      >
        <!-- Mobile: Number above title -->
        <div class="md:hidden">
          <SevenSegmentDisplay
            value={String(startIndex + index + 1).padStart(2, '0')}
            ariaLabel={`${lang === 'es' ? 'Proyecto' : lang === 'no' ? 'Prosjekt' : lang === 'ja' ? 'プロジェクト' : 'Project'} ${startIndex + index + 1}`}
            inverted={true}
            className="project-number !min-h-[2.05rem] !min-w-[3rem] !px-1 !py-0.5 !gap-[1px] origin-left [transform:scaleX(0.9)_scaleY(1.08)]"
          />
        </div>

        <!-- Number + Name (Desktop) / Just Name (Mobile) -->
        <div class="flex-[0_0_100%] md:flex-[0_0_30%] self-start flex gap-6 items-center">
          <!-- Desktop Number -->
          <div class="hidden md:inline-block w-[50px]">
            <SevenSegmentDisplay
              value={String(startIndex + index + 1).padStart(2, '0')}
              ariaLabel={`${lang === 'es' ? 'Proyecto' : lang === 'no' ? 'Prosjekt' : lang === 'ja' ? 'プロジェクト' : 'Project'} ${startIndex + index + 1}`}
              inverted={true}
              className="project-number !min-h-[2.05rem] !min-w-[3rem] !px-1 !py-0.5 !gap-[1px] origin-left [transform:scaleX(0.9)_scaleY(1.08)]"
            />
          </div>

          <div class="flex-1">
            <h3 class="h3 !my-0">
              {project.data.name}
            </h3>
          </div>
        </div>

        <!-- Description -->
        <div class="flex-1 self-start">
          <p class="text-body">
            {project.data.hook}
          </p>
        </div>

        <!-- Arrow Icon -->
        <div class="hidden md:flex flex-[0_0_48px] items-center justify-end">
          <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="text-main/80 project-arrow transition-[transform,opacity,color] duration-180 ease-out group-hover:translate-x-2 group-hover:-translate-y-2 group-hover:scale-[1.04]">
            <path d="M7 7h10v10"></path>
            <path d="M7 17 17 7"></path>
          </svg>
        </div>
      </a>
    ))}
  </div>
)}

<style>
  .project-row {
    border-left-width: 0;
    border-left-style: solid;
    animation: projectRowIn 360ms cubic-bezier(0.22, 1, 0.36, 1) both;
    animation-delay: calc(var(--row-index, 0) * 26ms);
    will-change: transform, opacity;
  }

  .project-row::before {
    content: '';
    position: absolute;
    inset: 0;
    background: linear-gradient(90deg, color-mix(in srgb, var(--color-main) 10%, transparent), transparent 60%);
    opacity: 0;
    transform: translateX(-14px);
    transition: opacity 170ms ease, transform 210ms cubic-bezier(0.22, 1, 0.36, 1);
    pointer-events: none;
  }

  .project-number {
    transition: transform 170ms cubic-bezier(0.22, 1, 0.36, 1), color 170ms ease;
  }

  .project-row:hover,
  .project-row:focus-visible {
    transform: translate(8px, -1px);
    border-left-width: 3px;
    box-shadow: inset 0 0 0 1px color-mix(in srgb, var(--color-main) 24%, transparent);
  }

  .project-row:active {
    transform: translate(3px, 0);
  }

  .project-row:hover::before,
  .project-row:focus-visible::before {
    opacity: 1;
    transform: translateX(0);
  }

  .project-row:hover .project-number,
  .project-row:focus-visible .project-number {
    transform: translateX(3px);
    color: var(--color-main);
  }

  .project-row:hover .project-arrow,
  .project-row:focus-visible .project-arrow {
    opacity: 1;
    color: var(--color-main);
  }

  @keyframes projectRowIn {
    from {
      opacity: 0;
      transform: translateY(6px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .project-row {
      animation: none;
    }

    .project-row,
    .project-row:hover,
    .project-row:focus-visible,
    .project-row:active,
    .project-row:hover .project-number,
    .project-row:focus-visible .project-number,
    .project-row:hover .project-arrow,
    .project-row:focus-visible .project-arrow {
      transform: none;
    }

    .project-row::before {
      transition: none;
      transform: none;
    }
  }
</style>
