---
interface Props {
  class?: string;
  pressed?: boolean;
}

const { class: className, pressed = false, ...rest } = Astro.props;
---

<kbd {...rest} class:list={['kbd', { 'kbd-pressed': pressed }, className]}>
  <slot />
</kbd>
